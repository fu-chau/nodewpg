const name = 'Robert';
const age = 27;
const phoneNumber = 666517777;
const street = 'Corydon';
const postalCode = 'R3L 0P3';
const married = false;

function userInfo() {
    const name = 'Robert';
    const age = 27;
    const phoneNumber = 666517777;
    const street = 'Corydon';
    const postalCode = 'R3L 0P3';
    const married = false;

    console.log (`user name ${name}`)
    console.log (`user age ${age}`)
    console.log (`user phone number ${phoneNumber}`)
    console.log (`user street name ${street}`)
    console.log (`user postal code ${postalCode}`)
    console.log (`is user married ${married}`)
}

userInfo();

// Define a function named showUser that will output the following data:
// Your name
// Your age
// Your phone number
// Your street
// Your postal code
// If you're married or not (boolean)