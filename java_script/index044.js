const text = 'HELLO'
const result = text.charAt(4) + text.charAt(3) + text.charAt(2) + text.charAt(1) + text.charAt(0);

console.log (result.toLowerCase());
console.log ('It kind of sound spanish, right?.. ole!!! (lol)\n hmmm... nooo not really ;)');